{
  "name": "restaurant-template-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "nodemon src/app.js",
    "start": "node init-db.js && node src/app.js",
    "build": "echo 'Backend has no build step'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "pg": "^8.8.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.0"
  }
}